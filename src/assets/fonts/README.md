# Inter variable fonts

Place the `Inter-roman.var.woff2` and `Inter-italic.var.woff2` binaries in this directory locally so the global `@font-face` rules in `src/assets/main.css` can load them. The actual font files are kept out of version control per request.
